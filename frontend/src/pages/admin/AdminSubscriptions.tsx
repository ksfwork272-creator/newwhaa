import { useMutation,useQuery,useQueryClient } from "@tanstack/react-query";import { Button } from "@/components/ui/button";import { Card,CardContent } from "@/components/ui/card";import { apiGet,apiPost,fmtMoney,type AdminRestaurant } from "@/lib/api";
export default function AdminSubscriptions(){const c=useQueryClient();const q=useQuery({queryKey:["admin-subscriptions"],queryFn:()=>apiGet<AdminRestaurant[]>("/admin/subscriptions")});const refresh=()=>void c.invalidateQueries({queryKey:["admin-subscriptions"]});const pay=useMutation({mutationFn:(id:string)=>apiPost(`/admin/restaurants/${id}/payment`),onSuccess:refresh});const remind=useMutation({mutationFn:(id:string)=>apiPost("/admin/reminders",{restaurant_id:id})});return <div data-testid="admin-subscriptions-page" className="space-y-6"><div><p className="text-xs font-bold uppercase tracking-[.2em] text-[#D94833]">Revenue operations</p><h1 data-testid="admin-subscriptions-heading" className="mt-2 font-heading text-4xl font-extrabold">Subscriptions</h1></div><div className="grid gap-4">{(q.data||[]).map(r=><Card key={r.id}><CardContent className="flex flex-wrap items-center justify-between gap-4 p-5"><div><p className="font-heading text-lg font-bold">{r.name}</p><p className="text-sm text-muted-foreground">Expires {r.subscription.end_date} · {r.subscription.days_remaining} days · {fmtMoney(r.subscription.monthly_price)}</p></div><div className="flex items-center gap-2"><span className="rounded-full bg-muted px-3 py-1 text-xs font-bold">{r.subscription.status}</span><Button data-testid={`remind-${r.id}`} variant="outline" onClick={()=>remind.mutate(r.id)}>Send Reminder</Button><Button data-testid={`payment-${r.id}`} onClick={()=>pay.mutate(r.id)} className="bg-[#2C614F]">Payment Received</Button></div></CardContent></Card>)}</div></div>}