import { useQuery } from "@tanstack/react-query";
import { Building2, CircleDollarSign, Clock3, ShoppingBag, Users } from "lucide-react";
import { Card, CardContent } from "@/components/ui/card";
import { apiGet, fmtMoney, type AdminSummary } from "@/lib/api";
const stats=["total_restaurants","active_restaurants","expiring_soon","expired","mrr","total_orders","today_orders","total_customers"] as const;
const labels:Record<string,string>={total_restaurants:"Total Restaurants",active_restaurants:"Active",expiring_soon:"Expiring Soon",expired:"Expired",mrr:"Monthly Recurring Revenue",total_orders:"Total Orders",today_orders:"Today's Orders",total_customers:"Total Customers"};
export default function AdminDashboard(){const q=useQuery({queryKey:["admin-summary"],queryFn:()=>apiGet<AdminSummary>("/admin/summary")});return <div data-testid="admin-dashboard" className="space-y-7"><div><p className="text-xs font-bold uppercase tracking-[.2em] text-[#D94833]">Platform overview</p><h1 data-testid="admin-dashboard-heading" className="mt-2 font-heading text-4xl font-extrabold">Super Admin</h1><p className="mt-2 text-muted-foreground">Restaurants, recurring revenue and service health in one place.</p></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{stats.map((key,index)=><Card data-testid={`admin-stat-${key}`} key={key} className="rounded-2xl"><CardContent className="p-5"><div className="flex items-center justify-between"><span className="text-xs font-bold uppercase tracking-wider text-muted-foreground">{labels[key]}</span>{index<2?<Building2 size={17}/>:index===4?<CircleDollarSign size={17}/>:index>6?<Users size={17}/>:index>4?<ShoppingBag size={17}/>:<Clock3 size={17}/>}</div><p className="mt-3 font-heading text-3xl font-extrabold">{key==="mrr"?fmtMoney(q.data?.[key]):q.data?.[key]??"—"}</p></CardContent></Card>)}</div></div>}