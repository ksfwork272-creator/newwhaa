import { useQuery } from "@tanstack/react-query";import { Card,CardContent } from "@/components/ui/card";import { apiGet,fmtMoney,type BillingResponse } from "@/lib/api";
export default function Billing(){const q=useQuery({queryKey:["billing"],queryFn:()=>apiGet<BillingResponse>("/billing")});const s=q.data?.subscription;return <div data-testid="billing-page" className="mx-auto max-w-4xl space-y-6"><div><p className="text-xs font-bold uppercase tracking-[.2em] text-[#2C614F]">Account access</p><h1 data-testid="billing-heading" className="mt-2 font-heading text-4xl font-extrabold">Billing & Subscription</h1></div>{s&&<Card className="rounded-2xl"><CardContent className="grid gap-5 p-6 sm:grid-cols-3"><div><p className="text-xs text-muted-foreground">Current plan</p><p className="font-bold">{s.plan}</p></div><div><p className="text-xs text-muted-foreground">Monthly</p><p className="font-bold">{fmtMoney(s.monthly_price)}</p></div><div><p className="text-xs text-muted-foreground">Status</p><p className="font-bold">{s.status}</p></div><div><p className="text-xs text-muted-foreground">Started</p><p>{s.start_date}</p></div><div><p className="text-xs text-muted-foreground">Expires</p><p>{s.end_date}</p></div><div><p className="text-xs text-muted-foreground">Days remaining</p><p>{s.days_remaining}</p></div>{["EXPIRED","SUSPENDED"].includes(s.status)&&<div data-testid="subscription-blocked-message" className="sm:col-span-3 rounded-xl bg-rose-50 p-4 text-rose-800">Your subscription has expired. Please complete your payment to continue using the platform.</div>}</CardContent></Card>}</div>}