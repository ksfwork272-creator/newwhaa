import { Link } from "react-router-dom";
import { useQuery } from "@tanstack/react-query";
import { Users } from "lucide-react";
import { Card, CardContent } from "@/components/ui/card";
import { apiGet, fmtMoney, type Customer } from "@/lib/api";
export default function Customers() { const query = useQuery({ queryKey: ["customers"], queryFn: () => apiGet<Customer[]>("/customers") }); return <div data-testid="customers-page" className="mx-auto max-w-6xl space-y-6"><div><p className="text-xs font-bold uppercase tracking-[0.2em] text-[#2C614F]">Audience</p><h1 data-testid="customers-heading" className="mt-2 font-heading text-4xl font-extrabold">Customers</h1><p className="mt-2 text-[#6E736D]">Everyone who has ordered from your restaurant.</p></div><Card className="overflow-hidden rounded-2xl border-[#E5E7E2]"><CardContent className="p-0"><div className="grid grid-cols-12 border-b border-[#E5E7E2] px-5 py-3 text-xs font-bold uppercase tracking-wider text-[#6E736D]"><div className="col-span-6">Customer</div><div className="col-span-3">Orders</div><div className="col-span-3 text-right">Spent</div></div>{(query.data || []).map((customer) => <Link data-testid={`customer-row-${customer.id}`} key={customer.id} to={`/customers/${customer.id}`} className="grid grid-cols-12 items-center border-b border-[#E5E7E2] px-5 py-4 hover:bg-[#F9FAF8]"><div className="col-span-6 flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-full bg-[#EAF4ED] font-bold text-[#2C614F]">{(customer.name || "?")[0]}</span><span className="truncate font-semibold">{customer.name || "Unknown"}</span></div><span className="col-span-3 text-sm">{customer.total_orders || 0}</span><span className="col-span-3 text-right font-heading font-bold">{fmtMoney(customer.total_spent)}</span></Link>)}{!query.isLoading && !(query.data || []).length && <div data-testid="customers-empty" className="grid place-items-center p-12 text-center text-[#6E736D]"><Users /><p className="mt-3">No customers yet.</p></div>}</CardContent></Card></div>; }